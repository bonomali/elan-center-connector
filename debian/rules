#!/usr/bin/make -f
export DH_VERBOSE=1


override_dh_systemd_start:
	dh_systemd_start --restart-after-upgrade

override_dh_systemd_enable:
	dh_systemd_enable --name=axon
	dh_systemd_enable --name=axon-websocket-proxy
%:
	dh $@ --with systemd
